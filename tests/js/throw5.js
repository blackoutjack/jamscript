// This tests the case in which an exception is thrown within a
// introspect handler, and the introspect block is the final statement
// in the program. This exercises specific interpreter code.
function pol(tx) {
  var node = ws[i];
}
introspect(pol) {
  var x = 1;
}
